#ifndef SPLASH_UTILS_HPP
#define SPLASH_UTILS_HPP

#include <string>

namespace Splash::Utils {
    // Return a string representing the given ARGB integer in hexadecimal
    std::string intToHexString(unsigned int);
};

#endif